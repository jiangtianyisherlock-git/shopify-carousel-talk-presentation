import SectionTransition from "../../components/SectionTransition";

export default function ReflectionTransition() {
  return <SectionTransition
    eyebrow="CHAPTER 04 · REFLECTION & NEXT"
    title="最后，"
    accent="说说心得和后续计划。"
    note="从这次实践里的关键反思，走向模块化能力的下一轮迭代。"
    leftLabel="LESSONS LEARNED"
    rightLabel="WHAT'S NEXT"
  />;
}
